{
  "name": "my-app1",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3005/",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.14",
    "concurrently": "^3.5.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "dev": "concurrently \"cd server npm run server\" \"npm start\""
  }
}
